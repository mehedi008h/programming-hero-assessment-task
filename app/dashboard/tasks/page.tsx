import { TaskFilterBar } from "@/components";
import React from "react";

const TasksPage = () => {
    return (
        <div>
            <TaskFilterBar />
        </div>
    );
};

export default TasksPage;
